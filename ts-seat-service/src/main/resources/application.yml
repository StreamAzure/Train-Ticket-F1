server:
  port: 18898

spring:
  application:
    name: ts-seat-service

swagger:
  controllerPackage: seat.controller

opentracing:
  jaeger:
    udp-sender:
      host: jaeger
      port: 6831
    const-sampler:
      decision: true # 固定采样，全部上报